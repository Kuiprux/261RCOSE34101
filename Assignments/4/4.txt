운영체제 실습 과제 4

Process_01
</1>: create
</2>: &tid, NULL, magic_box, (void*)(intptr_t)10
</3>: join
</4>: tid, &new_number

Process_02
</1>: exit
</2>: create
</3>: &tids[i], NULL, worker, &main_static
</4>: join
</5>: tids[i]

Process_03
</1>: exit
</2>: void*
</3>: create
</4>: &tids[i], NULL, worker, NULL
</5>: join
</6>: tids[i], &progress

Process_04
</1>: mutex_lock
</2>: &lock
</3>: mutex_unlock
</4>: &lock
</5>: create
</6>: &tids[i], NULL, worker, NULL
</7>: join
</8>: tids[i], &progress

Process_05
1-6: 코드에 사용되는 함수들이 정의된 헤더 파일을 컴파일 시 포함한다
7: 작업 수를 나타내는 NUM_TASKS 상수를 정의한다.
8: spread_words와 subordinate에서 sleep할 시간 SPREADING을 정의한다.
10: 남은 task의 수를 저장하는 cnt_task를 정의한다. 초기값은 NUM_TASKS이다. 여기서 _Atomic을 사용했는데, 이는 cnt_task--시 읽고 -1하고 저장하는 단계가 원자적으로 처리되게끔 하며, cnt_task > 0을 지속적으로 확인할 때도 항상 최신 값을 읽을 수 있게끔 메모리 동기화를 보장한다. 즉, 두 부분에서 data race 없이 일관적으로 접근하고 수정할 수 있게끔 보장한다.
12: spread_words 함수를 정의한다.
13: SPREADING 초만큼 실행을 중단한다.
14: “[subordinate] spreading words...\n”를 출력한다.
15: cnt_task 값을 감소시킨다.
18: king 스레드로부터 생성되는 subordinate 스레드의 진입점 함수 subordinate를 정의한다.
19: SPREADING 초만큼 실행을 중단한다.
20: "[subordinate] as you wish\n"를 출력한다.
21: 세 번 22번 문장을 실행한다. 현재 코드는 원문 그대로 i < 3을 사용하지만, i<NUM_TASKS를 사용한다면 NUM_TASKS 값이 바뀌어도 안정적으로 NUM_TASKS만큼 22번 문장을 실행할 수 있을 것이다.
22: spread_words를 호출한다.
25: 스레드의 역할이 끝났으니 종료한다.
28: 메인 스레드에서 생성되는 king 스레드의 진입점 함수 king을 정의한다.
29: 생성된 스레드의 id를 저장할 tid 변수를 생성한다.
30: 스레드 생성 결과를 저장할 status 변수를 생성한다.
31: "spread the words "를 출력한다.
34: 새 스레드(subordinate)를 생성하고, 진입점 함수는 subordinate로 한다. 스레드 생성 결과는 status 변수에 저장한다.
35: status 값이 0이 아닌 경우는 스레드 생성에 문제가 있었다는 의미이므로 36-37을 실행한다.
36-37: “error”를 출력하고 현재 스레드(king)를 종료한다. 
40: subordinate 스레드를 detach한다. 이제 해당 스레드는 분리되었으므로 join할 수 없다.
42: "that I am king!\n"을 출력한다.
43: 스레드의 역할이 끝났으니 종료한다.
46: 프로그램의 진입점 함수를 정의한다.
47: 생성된 스레드의 id를 저장할 tid 변수를 생성한다.
48: 스레드 생성 결과를 저장할 status 변수를 생성한다.
50: 새 스레드(king)를 생성하고, 진입점 함수는 king으로 한다. 스레드 생성 결과는 status 변수에 저장한다.
51: status 값이 0이 아닌 경우는 스레드 생성에 문제가 있었다는 의미이므로 52-53을 실행한다.
52-53: “error”를 출력하고 프로그램을 리턴 값 -1과 함께 종료한다. 리턴 값이 0이 아니므로 프로그램 실행에 문제가 있었음을 나타낸다.
55: king 스레드가 종료될 때까지 대기한다. king 스레드는 joinable이므로 pthread_join 함수로 join할 수 있다.
60: subordinate 스레드는 detached이므로 pthread_join 함수로는 join할 수 없다. 따라서 대신 while문으로 spread_words가 세 번 호출되어 cnt_task가 0이 될 때까지 대기한다. (busy waiting)
62: "The words have been spread...\n"를 출력한다.
63: 리턴값 0과 함께 프로그램을 종료한다. 리턴값 0은 프로그램이 정상 종료되었음을 나타낸다.

Process_06
1-6: 코드에 사용되는 함수들이 정의된 헤더 파일을 컴파일 시 포함한다
7: 작업 수를 나타내는 NUM_TASKS 상수를 정의한다.
8: spread_words와 subordinate에서 sleep할 시간 SPREADING을 정의한다.
10: 남은 task의 수를 저장하는 cnt_task를 정의한다. 초기값은 NUM_TASKS이다. 여기서 _Atomic을 사용했는데, thread_05와 달리 여기서는 cnt_task 접근 시 mutex를 사용하므로 mutex만으로도 data race를 막을 수 있다.
13: 공유 자원에 단일 스레드만 접근 가능하게끔 하는 mutex lock을 정의한다.
14: busy waiting을 제거하기 위해 원할 때 신호를 보내거나 신호가 올 때까지 대기할 수 있게 하는 cond를 정의한다.
16: spread_words 함수를 정의한다.
17: SPREADING 초만큼 실행을 중단한다.
18: “[subordinate] spreading words...\n”를 출력한다.
20: 공유 자원 cnt_task에 접근하기 전 lock을 걸어 다른 스레드에서 접근하지 못하게끔 한다.
21: cnt_task 값을 감소시킨다.
22, 24: subordinate 스레드에서 spread_words가 세 번 호출되어 cnt_task값이 0이 되면 cond로 대기 중인 스레드를 깨운다.
26: 공유 자원 cnt_task의 사용이 끝났으므로 lock을 해제하여 다른 스레드에서 접근 가능하게끔 한다.
29: king 스레드로부터 생성되는 subordinate 스레드의 진입점 함수 subordinate를 정의한다.
30: SPREADING 초만큼 실행을 중단한다.
31: "[subordinate] as you wish\n"를 출력한다.
32: 세 번 33번 문장을 실행한다. 현재 코드는 원문 그대로 i < 3을 사용하지만, i<NUM_TASKS를 사용한다면 NUM_TASKS 값이 바뀌어도 안정적으로 NUM_TASKS만큼 33번 문장을 실행할 수 있을 것이다.
33: spread_words를 호출한다.
36: 스레드의 역할이 끝났으니 종료한다.
39: 메인 스레드에서 생성되는 king 스레드의 진입점 함수 king을 정의한다.
40: 생성된 스레드의 id를 저장할 tid 변수를 생성한다.
41: 스레드 생성 결과를 저장할 status 변수를 생성한다.
42: "spread the words "를 출력한다.
45: 새 스레드(subordinate)를 생성하고, 진입점 함수는 subordinate로 한다. 스레드 생성 결과는 status 변수에 저장한다.
46: status 값이 0이 아닌 경우는 스레드 생성에 문제가 있었다는 의미이므로 47-48을 실행한다.
47-48: “error”를 출력하고 현재 스레드(king)를 종료한다. 
51: subordinate 스레드를 detach한다. 이제 해당 스레드는 분리되었으므로 join할 수 없다.
53: "that I am king!\n"을 출력한다.
54: 스레드의 역할이 끝났으니 종료한다.
57: 프로그램의 진입점 함수를 정의한다.
58: 생성된 스레드의 id를 저장할 tid 변수를 생성한다.
59: 스레드 생성 결과를 저장할 status 변수를 생성한다.
61-62: lock과 cond를 사용하기 전 초기화한다.
64: 새 스레드(king)를 생성하고, 진입점 함수는 king으로 한다. 스레드 생성 결과는 status 변수에 저장한다.
65: status 값이 0이 아닌 경우는 스레드 생성에 문제가 있었다는 의미이므로 66-67을 실행한다.
66-67: “error”를 출력하고 프로그램을 리턴 값 -1과 함께 종료한다. 리턴 값이 0이 아니므로 프로그램 실행에 문제가 있었음을 나타낸다.
69: king 스레드가 종료될 때까지 대기한다. king 스레드는 joinable이므로 pthread_join 함수로 join할 수 있다.
73: 공유 자원 cnt_task 값을 접근하기 전 lock을 걸어 cnt_task 값에 다른 스레드가 접근하지 못하게끔 한다.
74: subordinate 스레드는 detached이므로 pthread_join 함수로는 join할 수 없다. 따라서 대신 spread_words가 세 번 호출되어 cnt_task의 값이 0이 될 때까지 75를 반복한다. broadcast하지 않아도 wait에서 깨어날 수 있기 때문에, 그 경우에는 다시 대기 상태로 들어갈 수 있게끔 while 구문을 사용한다.
75: 다른 스레드에서 cond로 신호를 보내기 전까지 작동을 중지한다. (하지만 신호가 없어도 깨어나는 경우도 있다) 대기 중에는 lock이 풀리고 대기에서 깨어나면 다시 lock이 걸리므로, subordinate에서는 정상적으로 cnt_task 값을 감소시킬 수 있다.
77: cnt_task의 사용이 끝났으므로 걸어 두었던 lock을 해제한다.
80-81: 사용이 끝난 lock과 cond를 해제한다.
83: "The words have been spread...\n"를 출력한다.
84: 리턴값 0과 함께 프로그램을 종료한다. 리턴값 0은 프로그램이 정상 종료되었음을 나타낸다.
